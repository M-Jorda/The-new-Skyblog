{% extends 'base.html.twig' %}

{% block title %}Create a post{% endblock %}

{% block body %}
    {{ form_start(postForm, {'attr': {'class': 'form'}}) }}

    {{ form_widget(postForm, {'attr': {'class': 'form-group'}}) }}

    <button type="submit" class="btn btn-primary">Submit</button>

    {{ form_end(postForm) }}
{% endblock %}
